# implement the explainer recommender system

import subprocess

import numpy as np

from generate_perturbed_data import PerturbedDataGenerator


def recommender():

    # run_inception = ["python", "-m", "saliencyserieslab.explain", "--model", "inception"]
    # run_rocket = ["python", "-m", "saliencyserieslab.explain", "--model", "rocket"]
    # run_resnet = ["python", "-m", "saliencyserieslab.explain", "--model", "resnet"]

    # subprocess.run(run_inception)
    # subprocess.run(run_rocket)
    # subprocess.run(run_resnet)

    pass
